# Meta Lingua Production Environment Configuration for Iranian Self-Hosting
# Copy this file to .env on your Iranian server and update the values

# Database Configuration (REQUIRED)
# Replace with your local PostgreSQL connection string
DATABASE_URL=postgresql://username:password@localhost:5432/meta_lingua_production

# Authentication Configuration (REQUIRED)  
# Generate a strong secret key for JWT tokens
JWT_SECRET=your-super-secure-jwt-secret-key-here-make-it-very-long-and-random

# Application Environment
NODE_ENV=production

# Iranian SMS Service (Kavenegar)
KAVENEGAR_API_KEY=your-kavenegar-api-key
KAVENEGAR_SENDER=your-sender-number

# Ollama AI Service (Local Iranian Server)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.2

# File Upload Configuration
UPLOAD_MAX_SIZE=500MB
UPLOAD_DIR=./uploads

# Email Configuration (Iranian SMTP)
SMTP_HOST=mail.your-iranian-domain.ir
SMTP_PORT=587
SMTP_USER=admin@your-domain.ir
SMTP_PASSWORD=your-smtp-password

# Application Security
SESSION_SECRET=your-session-secret-key
CORS_ORIGIN=https://your-domain.ir

# VoIP Configuration (Isabel VoIP Line)
ISABEL_API_KEY=your-isabel-api-key
ISABEL_LINE_NUMBER=your-voip-line-number

# Application Settings
APP_URL=https://your-domain.ir
APP_NAME=Meta Lingua Academy
ADMIN_EMAIL=admin@your-domain.ir